[package]
name = "idcator"
version = "0.1.0"
edition = "2021"
description = "A library for a typed id"
license = "MIT"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
rand = "0.8.5"
serde = { version = "1.0.217", features = ["alloc", "derive"] }
thiserror = "2.0.11"
